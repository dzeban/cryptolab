#summary Список примитивов, которые надо реализовать
#labels Phase-Design

== Примитивы ==

В общем, здесь надо определить список того, что нам нужно будет сделать самим, а что есть в GMP.


|| *Примитив* || *Реализован в GMP* || *Комментарий* ||
||Простые арифметические операции|| resource gmp_add (resource a, resource b)||+,-,`*`,/,^,%||
||Модульная арифметика|| ||+(mod), -(mod), `*`(mod), /(mod), ^(mod)||
||Сравнения|| ||<,>,<=,>=, !=, ==||
||НОД|| resource gmp_gcd (resource a, resource b) ||
||НОК|| ||
||Мультипликативно обратное|| resource gmp_invert (resource a, resource b) ||
||Функция Эйлера|| ||
||Символ Якоби|| int gmp_jacobi (resource a, resource p) ||
||Символ Лежандра|| int gmp_legendre (resource a, resource p) ||
||Разложение на множители|| ||
||Поиск простых чисел|| ||
||Проверка на простоту|| int gmp_prob_prime (resource a [, int reps] ) ||
||Генерация случайных чисел|| ||
||Целочисленный корень|| resource gmp_sqrt (resource a) ||